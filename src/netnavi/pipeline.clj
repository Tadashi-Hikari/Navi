(ns netnavi.pipeline)

(defn check-plugin-config []
  )

; trigger stdin hooks?
(defn read-stdin 
  "This is a placeholder expression that should hold a representation to whatever the pipeline input was"
  [])

; trigger stdout hooks?
(defn read-stdout
  "This is a placeholder expression that should hold a representation to whatever the pipeline output was"
  [])



